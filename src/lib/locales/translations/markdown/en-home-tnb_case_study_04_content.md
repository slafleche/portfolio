Real backend work on top of the Payload framework. A custom admin page that
finds and bulk-corrects mismatched slugs, a recurring data-quality issue
from CMS imports. A search-pages-by-block utility that locates every page
using a given block type, useful when a content team needs to reindex or
repair a refactored block without grepping by hand. Node.js + TypeScript +
Payload data-model work on real editor problems.
