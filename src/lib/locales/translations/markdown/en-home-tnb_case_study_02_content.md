I authored a layered AI dev quality system specific to the agency's
Next.js + Payload + Tailwind stack. Claude Code and Cursor skills cover the
conventions a fresh AI session needs: where styles go, how tokens get named,
when to reach for Tailwind versus a CSS Module, what belongs in
`globals.css`, how CMS UI contracts get typed. Husky pre-commit hooks catch debugging artifacts and enforce the
rules before code reaches review. The skills travel with the codebase, so
any teammate who joins, human or AI, picks up the same conventions on day
one.
