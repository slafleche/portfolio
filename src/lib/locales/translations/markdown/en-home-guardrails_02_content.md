Teams move fast without the standards drifting with them. Each project gets
its own rule set (custom lint rules covering where styles live, how classes
and variables get named, when to reach for libraries versus building custom).
The drift the AI doesn't notice is the drift these rules catch.
