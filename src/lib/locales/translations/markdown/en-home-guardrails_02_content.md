Lint catches the small drift the AI doesn't know is drift. Each project gets
its own rule set: where each style lives, how classes and variables get named,
when to reach for a library like Tailwind and when to build it custom. I
architect that rule set so teams move fast without the standards drifting with
them.
