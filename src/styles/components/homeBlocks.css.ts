import { style } from '@vanilla-extract/css';

export const designSystems = style({});

export const aboutMe = style({});

export const approach = style({});

export const craft = style({});

export const projects = style({});
