import { style } from '@vanilla-extract/css';

export const photo = style({
	position: 'relative',
	width: '100%',
	height: '100%',
});

export const photoImage = style({
	display: 'block',
	position: 'relative',
	width: '100%',
	height: '100%',
});
